language: python

cache:
  directories:
  - $HOME/nats-server

python:
  - "3.9"
  - "3.10"
  - "3.11"

before_install:
  - bash ./scripts/install_nats.sh

before_script:
  - export PATH=$HOME/nats-server:$PATH

install:
  - pip install -e .[nkeys]

script:
  - make ci
  
notifications:
  email: false

sudo: true
dist: focal

jobs:
  include:
  - name: "Python: 3.11/uvloop"
    python: "3.11"
    install:
      - pip install uvloop
  allow_failures:
    - python: "3.11"
    - name: "Python: 3.11/uvloop"
