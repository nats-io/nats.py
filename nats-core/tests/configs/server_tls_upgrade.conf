# NATS server config for testing TLS upgrade after INFO
# Without handshake_first, server sends INFO first and expects client to upgrade
tls {
  cert_file: "./tests/certs/server-cert.pem"
  key_file: "./tests/certs/server-key.pem"
  ca_file: "./tests/certs/ca.pem"
  timeout: 2
}
