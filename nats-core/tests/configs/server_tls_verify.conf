# NATS server config for testing mTLS (mutual TLS with client cert verification)
tls {
  cert_file: "tests/certs/server-cert.pem"
  key_file: "tests/certs/server-key.pem"
  ca_file: "tests/certs/ca.pem"
  verify: true
  handshake_first: true
  timeout: 2
}
